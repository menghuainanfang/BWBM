# 带成分约束与未知位置权重的黑钉 Mastermind：开局查询的信息与决策价值

版本：2026-09-21，计算研究初稿。工作名称 BWBM 不代表已确立的文献术语。本文采用项目给定规则，尚未独立核实节目原始规则。本稿已有精确首问实验与多问蒙特卡洛结果；尚不足以作为完成全部验证的投稿稿。

## 摘要

考虑15个位置、5种符号且每种出现3次的隐藏答案，以及独立均匀选择的3个高权重位置。查询允许空白，反馈为加权位置匹配总分。本文将开局建模为有限状态空间上的实验设计，区分联合状态信息、答案信息、碰撞概率和最终作答效用。利用位置与符号对称性，将所有首问压缩为408类，并通过阶乘矩反演精确计算反馈分布。有限穷举显示，均衡首问在联合状态 Shannon 信息、平均剩余联合状态数及最坏剩余联合状态数三个指标上均最佳。随后比较十种固定开局设计的1—4问反馈熵，并建立微型模型的完整动态规划。结果不支持把首问结论直接外推为十轮最终得分最优性。

## 1. 模型与边界

令 Θ=(X,H)，X∈{0,…,4}^15，且每种符号出现3次；H是大小为3的位置子集。设 w_i=1+2·1{i∈H}。查询 q∈{−1,0,…,4}^15，−1为空白。内部反馈 S=Y/10=Σ_i w_i 1{q_i=X_i}。满分210。

基准先验明确取 X 均匀、H 独立均匀；这是一项建模选择，不是节目事实。答案空间 N_X=168,168,000，联合状态空间 N=76,516,440,000。对应熵约27.325328及36.155051 bit。

最终答卷要求完整，但题设没有要求提交向量也均衡，因此主模型允许任意完整答卷。代码提供 final_balanced 开关，用于单独研究附加约束版本。开局研究暂不引入侧信息，因其公布时点与内容尚未指定。10轮预算保留在配置中，当前实验只评价固定1—4问及微型两问策略。

## 2. 与已有问题的关系

普通 black-peg Mastermind 只返回位置正确的数量；本模型多了固定答案成分、未知权重和有限预算后的部分正确收益。AB-Mastermind 的“不重复符号”不同于这里每符号重复三次。Knuth的经典结果涉及黑白钉反馈，不能直接迁移。Kooi比较不同分割准则，适合作为实验方法参照。Goodrich讨论黑钉一致性与算法复杂性；其一般模型困难性并不自动证明当前受限模型同样困难。

Berger–Chute–Stone 与 Martinsson–Su 提供查询复杂度和线性表示视角；后一工作的 k=n 渐近结论不等于本模型15位置5符号下的常数保证。Twenty Questions 提醒我们：自由选择问题与受限查询族有本质差异。定量群体检测和称重模型比仅给出“是否含阳性”的布尔群体检测更接近加法反馈。完整书目、核验层级与引用追踪见《文献综述》。

## 3. 指标必须分开

给定固定查询矩阵 Q，令 Z=(S_1,…,S_t)，联合状态反馈桶为 C_z，p_z=|C_z|/N。无噪声下

I(Θ;Z)=H(Z)，H(Θ|Z)=log₂N−H(Z)。

平均剩余联合状态数为 NΣ_z p_z²；最坏剩余数为 N max_z p_z。前者等价于最大化二阶 Rényi 熵，后者等价于最大化无穷阶 Rényi 熵，不等价于 Shannon 熵。

答案目标的信息量应为 I(X;Z)=H(Z)−H(Z|X)。未知H使第二项通常非零。只剩一种X并不要求只剩一个(X,H)。定义答案唯一识别概率为 Σ_z p_z·1{|proj_X C_z|=1}。不能用蒙特卡洛样本中的单例桶代替真实唯一识别。

后验 b 下，定义 m_ia=E_b[w_i·1{X_i=a}]。当最终答卷不限成分时，最优期望得分

V₀(b)=10Σ_i max_a m_ia。

若最终答卷要求均衡，则要在每种符号使用次数约束下求最大权指派，不能逐位置独立选择。由于X和H在后验中可能相关，不能用 E[w_i]P(X_i=a) 替代 m_ia。

最坏保证得分为 max_a min_{θ∈supp(b)}Y(a;θ)，与上述期望目标不同。当前大模型未计算这个指标；不以最小样本得分冒充保证。

## 4. 首问对称性与精确分布

**命题1（证明）。** 在基准先验下，首问反馈分布只取决于各符号次数 q_a 及空白次数，不取决于排列。位置置换保持先验和评分；符号同时重命名也保持先验，因为各成分相同。因此所有均衡首问等价，AAABBBCCCDDDEEE在首问本身没有特殊的连续分块优势。

将五个非空符号次数排序且总和≤15，得到408种轨道代表。只有当成分相同、没有打破对称性的侧信息时，才能这样排序；一般成分向量不能随意排序合并。

令 M 为不加权正确位置数，(c)_j 为下降阶乘。对每个符号选择 j_a 个正确位置，J=Σ_a j_a，则

E[binom(M,J)] = (n−J)!/n! · Σ_{Σj_a=J} ∏_a binom(q_a,j_a)(c_a)_{j_a}。

证明：指定的J个位置匹配概率为 ∏_a(c_a)_{j_a}/(n)_J，再对指定位置子集求和。等价地，令多项式 P(z)=∏_aΣ_j binom(q_a,j)(c_a)_j z^j，系数为 A_J，则匹配子集总数 B_J=A_J(n−J)!/∏_a c_a!。由二项反演，恰好m个匹配的答案个数

D_m=Σ_{J≥m}(−1)^{J−m}binom(J,m)B_J。

条件于M=m，匹配位置中的高分位置数 L 服从 Hypergeom(n,m,r)，且 S=M+2L。因此精确联合状态计数

N_s=Σ_{m,l:m+2l=s} D_m binom(m,l)binom(n−m,r−l)。

代码使用整数完成计数，熵的对数计算用浮点数；不是对765亿状态逐一评分。

**计算结论1（固定实例，非一般参数定理）。** 408类首问中，(3,3,3,3,3)同时取得最大 Shannon 熵、最小碰撞概率、最小最大桶概率。

| 首问次数 | H(S), bit | Σp² | max p |
|---|---:|---:|---:|
| 3,3,3,3,3 | 3.322531907 | 0.112745760 | 0.144134068 |
| 4,3,3,3,2 | 3.317747801 | 0.113126471 | 0.144640010 |
| 14,0,0,0,0，加一空白 | 2.011928695 | 0.295368579 | 0.386813187 |
| 15,0,0,0,0 | 1.338230322 | 0.429421567 | 0.483516484 |

均衡首问后的平均剩余联合状态数仍约86.269亿。这里没有验证它最大化 I(X;S) 或最终作答效用。全部整数计数算法已与多个小模型全部查询的暴力枚举交叉核验。

## 5. 信息论界与结构性反例

**命题2（证明）。** 每问S∈{0,…,21}，故决策树分支数最多22。没有侧信息时，精确恢复Θ需要 t≥ceil(log₂N/log₂22)=9；仅恢复X需要 t≥ceil(log₂N_X/log₂22)=7。这些是必要条件，不是可达性证明。对答案恢复，每个X至少需要不同的一个叶子，因此仍成立，即使H是干扰变量。四问不可能对所有X保证完全恢复，但这不排除部分反馈唯一确定X，也不妨碍取得高分。

**命题3（证明）。** 任意事先固定 q，把每个非空符号依次循环改为其余四种，且q无空白，那么五次总分之和恒为210：每个位置恰好匹配一次。因此第五问由前四问线性确定，新增信息为零。即使每一行单独都有很高熵，联合信息也可能冗余。

**反例：学习权重不等于学习答案。** 全部填A时，M恒等于3，S=3+2|H∩{i:X_i=A}|。对于任意固定X，反馈分布都相同，因此 I(X;S)=0，但 H(S)=1.338230322 bit。它学习的是与答案相关的权重关系，不直接减少X的边际不确定性。不能据此断言它对后续最终得分毫无价值。

## 6. 固定1—4问实验

基准首问b=(0,0,0,1,1,1,…,4,4,4)。比较重复、符号循环、位置循环、拆块、随机均衡、集中、重叠区间，以及候选池熵贪心、桶大小minimax贪心、训练样本终局价值贪心，共十种设计。前三种候选池方法选择完整固定前缀，不根据真实反馈自适应；只能称候选池内贪心，不能称全局最优。

拆块矩阵按 i=0,…,14 取 b_i=floor(i/3)，a_i=i mod 5，四行为 b、a、b+a mod5、b+2a mod5。这只是构造名称，不声称是严格正交阵。随机均衡只取一个固定设计实例，其三次评估重采样不能代表全部随机设计的方差。

候选池含50个随机均衡查询及25个单符号前缀查询。设计随机种子271828，训练先验样本20000、种子314159。三个独立评估种子101、202、303，每次100000个状态。详见CSV、summary.json及代码。

| 设计 | 1问 | 2问 | 3问 | 4问 |
|---|---:|---:|---:|---:|
| 重复首问 | 3.3213 | 3.3213 | 3.3213 | 3.3213 |
| 符号循环 | 3.3213 | 6.5891 | 9.7177 | 11.8874 |
| 位置循环 | 3.3213 | 6.3373 | 9.2414 | 11.8658 |
| 拆块 | 3.3213 | 6.6422 | 9.9424 | 13.0604 |
| 随机均衡 | 3.3213 | 6.5967 | 9.8838 | 13.0047 |
| 集中 | 2.0123 | 2.9585 | 3.7612 | 4.5511 |
| 重叠区间 | 2.4832 | 4.9525 | 7.4076 | 9.8267 |
| 池内熵贪心 | 3.3213 | 6.6412 | 9.9386 | 13.0672 |
| 池内minimax | 3.3213 | 6.5915 | 9.8916 | 13.0226 |
| 池内价值贪心 | 3.3213 | 6.6355 | 9.9313 | 13.0714 |

表中是三个评估样本的平均 plug-in 联合反馈熵。四问反馈稀疏，估计有向下偏差；三个种子间稳定不代表偏差小。尤其13.06与13.07的细微差距不能作为优劣结论。碰撞概率用 Σ n_z(n_z−1)/(N_s(N_s−1)) 无偏估计，另存CSV；样本最大桶只作描述，不能证明最坏情况。图见 figures/openings.pdf。

另以独立训练和测试样本学习每个反馈桶的逐位置最优动作，估计最终得分。训练100000，测试100000，重复三次；未出现的桶固定回退全A，报告未见桶比例。该实验比较一个具体可实施解码器，不是精确 Bayes 最优效用；四问稀疏导致估计困难，应把泛化误差与查询信息量分开。结果见 final_score_heldout.csv 和自动表。

## 7. 自适应策略与微型精确动态规划

在后验b上，剩余t次查询的最优值满足

V_t(b)=max_q Σ_s P(s|b,q)V_{t−1}(b_{q,s})。

这是有限时域信念状态动态规划。侧信息若为随机变量R，应在对应轮次插入对R的期望与后验更新；未给定公布机制前，不能计算完整十轮最优策略。最坏保证版本将结果分支上的期望改为最小值，并用最坏终局效用。

代码对 n=3,c=(1,1,1),r=1,λ=3 的18个联合状态遍历所有4³=64个查询（含空白）。两轮最优自适应和最优固定策略均可达到满分50，故该实例的期望终局得分适应性差距为0。这不说明其他参数下差距为0。DP采用穷举状态及动作、浮点期望，所谓精确指无采样和无动作剪裁；微小舍入误差仍存在。

## 8. 结论与下一阶段

目前最可靠的成果是首问对称性、精确计数公式、固定实例408类穷举以及循环查询冗余。课程论文应围绕这些可核验内容，避免承诺完整求解游戏。下一步依序：(1)精确计算首问答案互信息及终局价值；(2)小规模寻找熵最优与价值最优的不同动作、以及正的适应性差距；(3)用查询列类型的生成函数精确计数2—4问，减少蒙特卡洛偏差；(4)引入已明确的侧信息机制；(5)完成更系统的后向/前向引文追踪和最近文献核验，再判断新颖性。

本稿未报告大模型真实唯一恢复概率、精确最坏终局得分或全局最优四问；这些是尚未完成的研究指标，不应填入推测数字。

### 补充：终局解码实验暴露的局限

独立测试中，拆块设计的三问/四问学习解码器平均得分约69.70/64.47，而区间设计约71.40/75.03。这里绝不能写成“多问降低最优得分”：真正的Bayes最优策略总能忽略新增反馈，故其值单调不降。下降说明按完整反馈桶独立估计动作的解码器样本不足（拆块四问未见桶约4.73%），也反映已有桶内估计噪声。该结果是计算方法的诊断，不是熵准则与真实最优价值排序相反的严格反例。下一版本应加入前缀回退、平滑或利用模型结构的后验推断，并独立选择解码超参数。
